version: "3"

services:
  main:
    build: .
    container_name: fastapi
    restart: always
    ports:
      - "8000:8000"

  rabbitmq:
    image: rabbitmq:4-management
    container_name: rabbitmq
    restart: always
    ports:
      - "5672:5672"
      - "15672:15672"
